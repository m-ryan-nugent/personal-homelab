apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard
  namespace: homelab
spec:
  selector:
    app: cluster-dashboard
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort