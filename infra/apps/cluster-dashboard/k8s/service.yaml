apiVersion: v1
kind: Service
metadata:
  name: cluster-dashboard
  namespace: homelab
spec:
  selector:
    app: cluster-dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
  type: NodePort