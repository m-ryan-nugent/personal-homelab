server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location = / {
        return 302 /frontend/;
    }

    location /frontend/ {
        try_files $uri $uri/ /frontend/index.html;
    }

    location /config/ {
        add_header Cache-Control "no-store";
        try_files $uri =404;
    }
}