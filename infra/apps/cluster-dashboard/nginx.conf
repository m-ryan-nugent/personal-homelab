server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;

    location = / {
        try_files /frontend/index.html =404;
    }

    location /frontend/ {
        try_files $uri $uri/ /frontend/index.html;
    }

    location /config/ {
        try_files $uri =404;
    }
}